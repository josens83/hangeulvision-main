import type { Metadata } from "next";
export const metadata: Metadata = { title: "Start Learning Korean" };
export default function L({ children }: { children: React.ReactNode }) { return children; }
